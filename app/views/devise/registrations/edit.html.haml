.max-w-md.mx-auto.bg-white.p-8.rounded-lg.shadow-md
  %h2.text-2xl.font-bold.text-center.mb-6 Edit Profile
  = form_for(resource, as: resource_name, url: registration_path(resource_name), html: { method: :put }) do |f|
    .mb-4
      = render "devise/shared/error_messages", resource: resource
    .mb-4
      = f.label :email, class: "block text-sm font-medium text-gray-700 mb-1"
      = f.email_field :email, autofocus: true, autocomplete: "email", class: "w-full px-3 py-2 border border-gray-300 rounded-md focus:outline-none focus:ring-blue-500 focus:border-blue-500"
    - if devise_mapping.confirmable? && resource.pending_reconfirmation?
      .mb-4.text-sm.text-yellow-600.bg-yellow-50.p-2.rounded
        Currently waiting confirmation for: #{resource.unconfirmed_email}
    .mb-4
      = f.label :password, class: "block text-sm font-medium text-gray-700 mb-1"
      %span.text-xs.text-gray-500 (leave blank if you don't want to change it)
      = f.password_field :password, autocomplete: "new-password", class: "w-full px-3 py-2 border border-gray-300 rounded-md focus:outline-none focus:ring-blue-500 focus:border-blue-500"
      - if @minimum_password_length
        %span.text-xs.text-gray-500
          (#{@minimum_password_length} characters minimum)
    .mb-4
      = f.label :password_confirmation, class: "block text-sm font-medium text-gray-700 mb-1"
      = f.password_field :password_confirmation, autocomplete: "new-password", class: "w-full px-3 py-2 border border-gray-300 rounded-md focus:outline-none focus:ring-blue-500 focus:border-blue-500"
    .mb-6
      = f.label :current_password, class: "block text-sm font-medium text-gray-700 mb-1"
      %span.text-xs.text-gray-500 (we need your current password to confirm your changes)
      = f.password_field :current_password, autocomplete: "current-password", class: "w-full px-3 py-2 border border-gray-300 rounded-md focus:outline-none focus:ring-blue-500 focus:border-blue-500"
    .mb-8.border-b.pb-8
      = f.submit "Update", class: "w-full flex justify-center py-2 px-4 border border-transparent rounded-md shadow-sm text-sm font-medium text-white bg-blue-600 hover:bg-blue-700 focus:outline-none focus:ring-2 focus:ring-offset-2 focus:ring-blue-500"
  .pt-2
    %h3.text-xl.font-semibold.mb-4.text-red-600 Cancel My Account
    %p.mb-4.text-sm.text-gray-600 Unhappy with our service? You can cancel your account below. This action cannot be undone.
    = button_to "Cancel my account", registration_path(resource_name), data: { confirm: "Are you sure?", turbo_confirm: "Are you sure?" }, method: :delete, class: "w-full flex justify-center py-2 px-4 border border-transparent rounded-md shadow-sm text-sm font-medium text-white bg-red-600 hover:bg-red-700 focus:outline-none focus:ring-2 focus:ring-offset-2 focus:ring-red-500"
  .mt-6.text-center
    = link_to "Back", :back, class: "text-blue-600 hover:text-blue-800"
